# The EXPERIMENT lane: re-run the measurement campaign. Installs the RESULTS
# lane too, so one environment covers both.
-r requirements.txt

pyspark==4.0.0
pydicom>=3.0
numpy>=1.26.0
pyyaml>=6.0.1
boto3>=1.34.0
tcia-utils>=2.5

# Development and testing
pytest>=8.1.0
pytest-cov>=5.0.0
